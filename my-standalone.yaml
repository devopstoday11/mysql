apiVersion: kubedb.com/v1alpha1
kind: MySQL
metadata:
  name: my-standalone
spec:
  version: "8.0.14"
  replicas: 1
  storageType: Durable
  storage:
    storageClassName: "standard"
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  terminationPolicy: WipeOut